{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["cloudProvider"],
  "properties": {
    "cloudProvider": {
      "type": "string",
      "enum": ["gcp", "aws", "local"]
    }
  }
}