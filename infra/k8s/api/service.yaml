apiVersion: v1
kind: Service
metadata:
  labels:
    app: job-service-api
  name: job-service-api
spec:
  ports:
    - port: 6500
      protocol: TCP
      targetPort: 6500
  selector:
    app: job-service-api
status:
  loadBalancer: {}
